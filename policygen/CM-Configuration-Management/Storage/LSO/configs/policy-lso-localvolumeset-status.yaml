apiVersion: local.storage.openshift.io/v1alpha1
kind: LocalVolumeSet
metadata:
  name: localvolumeset
  namespace: openshift-local-storage
status:
  totalProvisionedDeviceCount: '{{ fromConfigMap "openshift-config" "cluster-info" "odf_target_disk_count" | toInt }}'