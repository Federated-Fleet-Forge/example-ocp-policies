kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: image-registry-single
  namespace: openshift-image-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: hpe-storage-reduce
  volumeMode: Filesystem
