# Source: hub cluster pull-secret (openshift-config). Requires PolicyGenerator to run with hub kubeconfig.
---
apiVersion: v1
kind: Secret
metadata:
  name: pull-secret
  namespace: policies
type: Opaque
data:
  .dockerconfigjson: '{{ (fromSecret "openshift-config" "pull-secret" ".dockerconfigjson") }}'
